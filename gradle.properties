# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true
# Cap the worker count so a build does not saturate the machine.
org.gradle.workers.max=4
# Parallel sync, Gradle 9.4+.
org.gradle.tooling.parallel=true

# Kotlin code style for this project: "official" or "obsolete".
kotlin.code.style=official
# Avoids "Could not connect to Kotlin compile daemon" by compiling in-process.
kotlin.compiler.execution.strategy=in-process

# Each library's R class contains only its own resources, which keeps the R class small.
android.nonTransitiveRClass=true
