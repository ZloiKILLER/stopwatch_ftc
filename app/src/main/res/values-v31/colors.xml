<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      From Android 12 the launch window follows the wallpaper palette too, so the splash does not
      flash a brand colour before the dynamic Compose theme takes over.
    -->
    <color name="window_background">@android:color/system_neutral1_10</color>
</resources>
